# Optional fallback for local development only.
# Preferred flow: each user adds provider name + key in Settings -> AI provider & key.

LLM_PROVIDER_NAME=your_provider_name_here
LLM_API_KEY=your_api_key_here
